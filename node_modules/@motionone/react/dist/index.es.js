import { createMotionComponent } from './component.es.js';

const components = new Map();
const motion = new Proxy({}, {
    get: (_, key) => {
        !components.has(key) && components.set(key, createMotionComponent(key));
        return components.get(key);
    },
});

export { motion };
